allOf:
  - $ref: "Sensor.yaml"
  - type: object
    description: GNSS sensor.
    properties:
      type:
        description: NGSI entity type. It must be equal to `GNSS`
        type: string
        enum:
          - GNSS
        x-ngsi:
          type: Property
    required:
      - id
      - type